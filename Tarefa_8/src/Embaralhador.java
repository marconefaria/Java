	/*interface que representa classes reponsáveis por receber uma palavra e
	retornar ela embaralhada. Pelo menos duas implementações deverão ser feitas.*/

public interface Embaralhador {
	//inverter string, permutar randomicamente, trocar letras impares por pares e etc...
	public String inverter(String palavra);
	public String trocarLetras(String palavra);
}
